-> Required Sections
Rudraksh Git, GitHub, Postman Assessment

-> Tools used (Git, GitHub, Postman)

-> APIs tested - GET {{baseUrl}}/albums - GET {{baseUrl}}/photos - GET {{baseUrl}}/posts/1 - POST {{baseUrl}}/posts - PUT {{baseUrl}}/posts/1/userId=1 - DELETE {{baseUrl}}/posts/1

Steps to run:

1. Clone repo
2. Import Postman collection
3. Select environment
4. Run requests

-> Git commands used - git init - git add . - git commit -m "Initial commit" - git status - git push
